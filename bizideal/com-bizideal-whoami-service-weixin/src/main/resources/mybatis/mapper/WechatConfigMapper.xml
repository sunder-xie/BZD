<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bizideal.whoami.weixin.mapper.WechatConfigMapper">

	<select id="getConfigByProfile" resultType="com.bizideal.whoami.weixin.entity.WechatConfig"
	parameterType="string">
		SELECT * FROM wechat_config WHERE wechat_profile=#{profile}
	</select>

</mapper>